#!/usr/bin/env php
<?php
#Load composer libs for bitbucket api
require_once(dirname(__DIR__) . '/vendor/autoload.php');

$root = dirname(__DIR__);
$result = system('git diff-index --quiet HEAD --',$ret_val);
echo $ret_val;
echo "\n";
echo true;
?>
